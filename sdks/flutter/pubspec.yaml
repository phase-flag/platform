name: phaseflag_flutter
description: Phase Flag Flutter SDK for feature flag evaluation with local caching, targeting rules, and event tracking.
version: 0.1.0
homepage: https://github.com/phaseflag/sdk-flutter

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
